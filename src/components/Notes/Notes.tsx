import React from 'react';

import { Typography } from '@mui/material';

function Notes() {
  return (
    <div>
      <Typography
        variant="h1"
        color="primary"
        align="center"
      >
        Notes page
      </Typography>
    </div>
  );
}

export default Notes;
